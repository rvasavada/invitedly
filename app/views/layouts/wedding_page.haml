!!!
%html{lang: "en"}    
  %head
    %title="#{@occasion.name} | Invitedly"
    = stylesheet_link_tag "application", media: "all"
    = analytics_init if Rails.env.production?
    
    = csrf_meta_tags
    =favicon_link_tag "/assets/favicon.ico"
    %body#wedding
      .container
        .row
          .col-xs-12
            %ul.list-inline.pull-right
              - if user_signed_in? 
                %li= link_to "Edit Wedding Details", edit_occasion_path(current_user.occasion), class: "signout-link"
                %li= link_to "Back to Guests", occasion_contacts_path(current_user.occasion), class: "signout-link"
            
                %li= link_to "Settings", edit_user_registration_path, class: "signout-link"
                %li= link_to "Sign out", destroy_user_session_path, method: :delete, class: "signout-link"

        .row
          .col-xs-12
            - if notice.present?
              .alert.alert-danger
                = notice.html_safe
        .row
          .col-xs-12
            = yield

      = javascript_include_tag "application"