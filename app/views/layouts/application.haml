!!!
%html{lang: "en"}    
  %head
    %title Invitedly
    = stylesheet_link_tag "application", media: "all"
    = analytics_init if Rails.env.production?
    
    = csrf_meta_tags
    =favicon_link_tag "/assets/favicon.ico"
    %body{style:"padding-top: 20px;"}
      .container#main

        - if notice.present?
          .alert.alert-success
            = notice
        - if alert.present?
          .alert.alert-danger
            = alert

        .fully-container
          .row
            .col-xs-12
              = yield
        #footer
          #legal Invitedly &copy; 2014.  All Rights Reserved.

      = javascript_include_tag "application"      