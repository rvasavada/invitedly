= form_for @occasion, :html => { :role => "form"} do |f|
  .panel.panel-default
    .panel-heading
      %h4.panel-title Occasion details
    .panel-body
      .form-group{class: @occasion.errors.include?(:name) ? 'has-error' : ''}
        = f.label :name, class: "control-label" do
          Name
          %span{style: "color:red"}*
        = f.text_field :name, :autofocus => true, :maxlength => 50, class: "form-control"
        %span.help-block= @occasion.errors.get(:name).join('; ')
    
      .form-group{class: @occasion.errors.include?(:slug) ? 'has-error' : ''}
        = f.label :slug, class: "control-label" do
          Occasion Handle
          %span{style: "color:red"}*
        = f.text_field :slug, :placeholder => "johnsbirthday", :maxlength => 50, class: "form-control"
        %span.help-block= @occasion.errors.get(:slug).join('; ')

      .form-group{class: @occasion.errors.include?(:description) ? 'has-error' : ''}
        = f.label :description, class: "control-label" do
          Description
          %span{style: "color:red"}*
        = f.text_area :description, :rows => 5, :maxlength => 300, :class => "form-control"
        %span.help-block= @occasion.errors.get(:description).join('; ')



  .pull-right
    = f.submit "Save occasion", :class => "btn btn-success btn-lg"
  = link_to 'Cancel', :back, :class=> "btn btn-default btn-lg"