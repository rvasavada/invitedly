.row
  .small-12.columns
    %h1 Occasions
.row
  .small-12.columns
    - if @occasions.present?
      .panel.panel-default
        .panel-body
    
          - @occasions.each do |occasion|
            .btn-group.pull-right
              %button.btn.btn-primary.dropdown-toggle{"data-toggle" => "dropdown"}
                Action
                %span.caret
              %ul.dropdown-menu{role: "menu"}
                %li= link_to "Edit occasion", edit_occasion_path(occasion)
                %li= link_to "Delete occasion", occasion, method: :delete, data: {confirm: 'Are you sure you want to delete this occasion?'}
      
            %h3
              %strong=link_to occasion.name, occasion
            %p.subheader= occasion.description
            %hr
          %p.text-center=link_to "Add another occasion!", new_occasion_path
    -else
      .panel.panel-default
        .panel-body
          %p.text-center
            You have no occasions.
            =link_to "Add an occasion!", new_occasion_path