%h2 Guestbook

%ul.list-group
  - if @invitations.present?
    - @invitations.each do |invitation|
      %li.list-group-item
        ="<strong>#{invitation.name}</strong>: #{invitation.message}".html_safe
        %br
        %small="#{time_ago_in_words(invitation.updated_at)} ago"
  - else
    %li.list-group-item.text-center
      %h4 No messages yet!