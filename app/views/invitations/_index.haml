%h2.form-h2
  Guest list
  %small="#{pluralize(@guests.count, 'guest')} found"

- @guests.each do |guest|
  %p
    =link_to guest.invite_name, occasion_invitations_path(@occasion, guest_id: guest), data: {remote: true}
    
%p.text-center#no_row_found
  %em No guests invited


