- if type == "attending"
  .text-success
    = response.join('<br>').html_safe
- elsif type == "not_attending"
  .text-danger
    = response.join('<br>').html_safe
- else type == "not_responded"
  = response.join('<br>').html_safe