.container
  %h1 
    .pull-right= link_to 'New Event', new_occasion_event_path(@occasion), class: "btn btn-success"
    Events


  - if @events.present?
    %ul.list-group
      - @events.each do |event|
        %li.list-group-item
          %h4
            .pull-right
              = link_to "Edit", edit_occasion_event_path(@occasion,event), class: "btn btn-default btn-sm"
              = link_to "Trash", occasion_event_path(@occasion,event), method: :delete, data: {confirm: "Are you sure you want to delete this event?"}, class: "btn btn-default btn-sm"
            =event.name
          %p=event.description
          %small.text-muted
            = "#{event.start_date.strftime('%A, %B %e, %Y')} at #{event.start_time}"
            %br
            - unless event.address_1.blank?
              = link_to event.location, "https://www.google.com/maps/preview#!q=#{event.address}"
              = "| #{event.address}"
            - else
              = event.location


  -else
    .well.text-center
      %h3 No events found.
      =link_to "Add an event!", new_occasion_event_path(@occasion)